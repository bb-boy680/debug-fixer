---
name: debug-fixer
description: 系统级 Bug 自主修复专家。覆盖逻辑与视觉缺陷，通过正确性假设、动态埋点、日志飞轮和源头修复实现高准确率修复。自适应复杂度，支持记忆学习与方向自疑。当用户提到 debug、bug fix、修复缺陷、排查异常、样式错误、视觉问题时使用此技能。
---

# DebugFixer — 自主修复 Bug 的 AI 技能

## 核心原则

1. **假设先行**：修复前必须建立"系统应该怎样运行"的预期，无假设不行动。每条假设附置信度（高/中/低），低置信度需向用户确认。
2. **向上追溯**：从异常点沿调用链/DOM 树反向追溯，不跳跃，直到找到数据或样式首次变质的节点。
3. **源头修复**：在错误首次产生的位置修改，禁止在崩溃处打补丁或用 `!important` 掩盖。
4. **本地判定**：每步判断偏差是本地产生（输入正确输出错误）还是上游传入，只有上游传入才继续追溯。
5. **最小侵入**：埋点首轮 ≤3 处，代码用 `#region DEBUG` 包裹，修复后一键清除。
6. **碰壁即疑**：连续两次修复失败或飞轮耗尽时，停止修改代码，启动方向自疑。

## 启动分析

收到 Bug 描述后，先分析再分流，不读任何参考文档：

### 第一步：检索记忆

从 `.debug/memory/` 按关键词匹配文件名，读取 1~3 份相关记忆。匹配到成功案例参考其假设与修复方式；匹配到失败案例主动避开其无效路径。

### 第二步：分析代码，重建调用链

**→ 必须先读 `references/root-cause-tracing.md`，按其中的 5 步法和逻辑/视觉链路方法分析**

定位异常相关的源文件，沿调用关系追踪，重建从入口到异常点的完整调用路径。视觉 Bug 同时沿 DOM 树和 CSS 继承链追踪。跨异步边界、中间件、monorepo 跨包调用都视为链上节点，不断裂。

输出调用链格式：
```
【调用链】
handleRequest → getCart → checkout → applyCoupon ← TypeError
```
视觉 Bug 格式：
```
【样式链】
AppLayout → SidebarWrapper(overflow:hidden,width:0) → Sidebar(display:none)
```

### 第三步：建立正确性假设

基于调用链上的类型声明、接口契约、命名语义、注释、测试用例、用户描述，提取每个关键节点"应该怎样运行"的预期。视觉 Bug 同时提取元素可见性、尺寸、位置、响应式断点等预期。每条假设附置信度（高/中/低），低置信度需向用户确认。

```
【正确性假设】
1. ✅ 高：getCart 对已登录用户应返回非 null Cart 对象。证据：接口契约
2. ⚠️ 中：applyCoupon 仅在 cart 有效时执行折扣。证据：函数名推测
3. ❓ 低：coupon.isValid() 过期时必返回 false。确认：边界情况？
```

### 第四步：分流

基于调用链和假设，判断能否确定源头：

- **源头可定位**：假设明确，调用链上偏差来源清晰 → 直接修复
- **源头不确定**：多个嫌疑点、假设冲突、需要运行时数据验证 → 启动飞轮

直接修复后被 Proceed → 自动升级为飞轮，不重新分析。

## 简单路径

1. 基于已建立的调用链和假设，直接源头修复
2. 告知用户修改内容，请用户验证

## 复杂路径 — 飞轮流程

### 1. 环境判定

对每个待埋点文件判定运行环境（客户端/服务端）：

- 优先读 `.debug/config.yaml` 的 glob 规则匹配
- 未命中则根据代码特征动态检测（浏览器 API → 客户端，Node 核心模块 → 服务端）
- 双端代码优先选服务端埋点（避免启动 HTTP 服务）
- 新规则写入 `.debug/config.yaml`（覆盖整个目录，非单个文件；检查不重复）

**→ 写埋点代码前必须读 `references/instrumentation-guide.md` 和 `references/environment-detection.md`**

### 2. 生成会话 & 启动日志服务

- 生成 `session_id`：`debug_YYYYMMDD_HHmmss_随机4位`
- 后台启动：`node scripts/debugger-server.js`（端口从 9220 递增）
- 从终端输出提取端口号 → 存为 `DEBUG_PORT`

### 3. 制定并注入埋点

- 按怀疑度排序（1~10），首轮 ≤3 处
- 怀疑度因素：距异常层数、数据变异程度、历史出错记录（+3）、代码复杂度、视觉遮挡概率、契约明确性违背（8~10）
- 根据环境选择模板（客户端 fetch / 服务端文件写入）
- 替换占位符后直接写入源码，`#region DEBUG` 包裹

### 4. 用户复现 → 收集日志

- 告知用户执行复现操作
- 读取 `.debug/logs/<session_id>.log`

### 5. 偏差分析 → 源头修复

**→ 分析追溯方法读 `references/root-cause-tracing.md`**

- 将日志与正确性假设对比
- 本地产生（输入正确输出错误）→ 当前节点即源头，直接修复
- 上游传入 → 追加 1~2 处埋点继续追溯
- 每轮分析后清空日志文件内容（保留空文件）

### 飞轮规则

- 最多 3 轮埋点（首轮 + 2 次追加），每轮追加 ≤2 处
- 首轮捕获偏差且本地产生 → 停止追加，源头修复
- 连续两轮未捕获偏差 → 触发方向自疑
- 修复完成后告知用户修改内容，请用户验证

## 方向自疑

### 触发条件（满足任一）

- 连续两次修复后用户反馈未解决
- 飞轮 3 轮耗尽仍无法定位源头
- 用户描述与代码/日志存在根本性矛盾

### 触发后动作

1. **立即停止所有代码修改**，输出 `[触发方向自疑审查]`
2. 读取 `references/direction-doubt.md` 和 `references/restart-strategy.md`
3. 执行"质疑的质疑"审查：

| 审查项 | 若"是"则暂缓质疑 |
|--------|-----------------|
| 高怀疑节点尚未埋点验证？ | 优先追加 1~2 处埋点 |
| 已接近源头？追加一轮可能锁定？ | 向用户说明后继续飞轮 |
| 用户的 Proceed 可能只是没看到效果？ | 说明进展后询问 |

4. 通过审查后按三层六维度质疑，向用户提出**具体追问**（不用泛泛建议）
5. 根据质疑层级选择重启策略

### 重启层级

| 层级 | 场景 | 动作 | session_id |
|------|------|------|-----------|
| 信号层 | 上下文缺失、环境差异 | 补全信息，继续当前飞轮 | 不变 |
| 理解层 | 假设根因错误、修复路径误判 | 废弃矛盾假设，重建假设，重新埋点 | 不变 |
| 系统层 | 架构缺陷、多 Bug 交织 | 完全重启（**需用户确认**） | 重新生成 |

系统层重启最多 **1 次**。重启后仍失败 → 输出最终建议，生成 `FAIL-FINAL-<标题>.md` 到 `.debug/memory/`。

重启后必须验证：保留的调用链/环境缓存/假设是否与新信息矛盾，矛盾则升级重启层级。

## 交互规则

**核心原则：AI 自己判断诊断结论，但每个回合结束时必须调用 AskUserQuestion 引导用户下一步。**

### AI 自主决策（不问用户）

- 找到源头 → 直接修复，不要问"是否修复"
- 不确定源头 → 启动飞轮用埋点验证，不要问"是不是这个原因"
- 修复后用户反馈无效 → 自动调整假设继续，不要问"要不要继续分析"

### 每回合收尾（强制）

每个回合结束时，必须调用 AskUserQuestion，选项固定为两个，不要新增、删除或修改：

- **Proceed**：Bug 未修复，AI 智能选择后续操作（追加埋点、调整假设、启动方向自疑等）
- **Mark Fixed**：Bug 已修复，执行清理链：关闭后台日志服务 → 删除日志文件 → 清理埋点代码 → 生成记忆文档

### Proceed 后的自动行为

AI 根据当前阶段自动决定下一步，不再询问：

| 当前阶段 | 下一步 |
|----------|--------|
| 修复后被 Proceed | 计为修复失败。有线索→调整假设继续；无线索→追加埋点；连续2次→方向自疑 |
| 埋点已注入后被 Proceed | 读取日志，进入偏差分析 |
| 方向自疑后被 Proceed | 按重启清单执行，重置失败计数器 |

### 自然语言映射

用户可能直接说话而非点击按钮，等价映射：
- "好了""修复了""没问题了" → Mark Fixed
- "还是不行""没效果""更差了" → Proceed（计为修复失败）

### 修复失败计数

连续 2 次修复后用户选 Proceed → 触发方向自疑。方向自疑审查通过且用户确认新方向后才可重置计数器。

## 记忆文档

Mark Fixed 后，读取 `references/memory-system.md`，生成脱敏记忆到 `.debug/memory/`：

**成功格式**：标题、时间、调用堆栈、错误源头、假设、修复方式
**失败格式**：标题、时间、飞轮轮次、已尝试假设、方向质疑、建议方向

**脱敏规则**：字段名含 password/secret/token/apiKey/credential → 值替换为 `[REDACTED]`；完整页面文本不写入；个人信息替换为 `[REDACTED]`。

## 环境清理

**每轮飞轮结束**：清空 `.debug/logs/<session_id>.log` 内容（保留空文件）

**Mark Fixed 时**（按顺序）：
1. 终止 `debugger-server` 进程（`pkill -f "debugger-server.js"` 或 `kill <PID>`）
2. 删除 `.debug/logs/<session_id>.log`
3. 执行 `node scripts/cleanup-debug-blocks.js --session-id <id> --files <文件列表>`
4. 生成记忆文档到 `.debug/memory/`

## .gitignore

首次创建 `.debug/` 目录时，检查项目根目录 `.gitignore` 是否已包含 `.debug/`，若未包含则追加。

---

## 按需加载参考文档

| 文档 | 何时读 | 原因 |
|------|--------|------|
| `references/root-cause-tracing.md` | 启动分析第二步（**必读**） | 5 步法与逻辑/视觉追溯方法论 |
| `references/instrumentation-guide.md` | 写埋点代码前 | 8 种语言埋点模板，必须照格式写 |
| `references/environment-detection.md` | 写埋点代码前 | 环境判定特征表与缓存规则 |
| `references/direction-doubt.md` | 触发方向自疑时 | 三层六维度质疑框架 |
| `references/restart-strategy.md` | 触发方向自疑时 | 分层重启策略 |
| `references/memory-system.md` | Mark Fixed 时 | 记忆文档格式与脱敏规则 |

**启动分析第二步必须加载 `root-cause-tracing.md`，其余文档按上表时机加载，不提前加载。**

## 工具脚本

- **`scripts/debugger-server.js`**：日志接收服务，端口从 9220 自动递增。后台启动：`node scripts/debugger-server.js`
- **`scripts/cleanup-debug-blocks.js`**：按 sessionId 清理埋点块。示例：`node scripts/cleanup-debug-blocks.js --session-id "debug_20240601_153000_abcd" --files "src/app.js" "src/utils.js" --log-file "/path/to/project/.debug/logs/debug_20240601_153000_abcd.log"`
