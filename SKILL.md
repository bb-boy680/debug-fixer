---
name: debug-fixer
description: 当用户报告 bug、异常行为、崩溃、测试失败、功能失效、UI 显示异常、样式错乱、点击无反应、数据不对、修复后仍不对，或行为与预期不符时使用。专注于准确定位 BUG 根因；若存在高置信、低风险、单点源头，允许一次首次修复；否则立即转入运行时证据和埋点；任何修复后都必须输出短确认回合，并用 session 和记忆保持上下文连续。
---

## 路径约定

本 skill 中所有 `references/`、`scripts/`、`assets/` 路径都相对于 `<skill base directory>` 解析。

---

# Debug Fixer

目标：像 Cursor Debug Mode 一样处理 BUG。先判断能不能直接修一次；不行就收证据；修完必须让用户确认；确认后再沉淀记忆。

## 核心规则

1. **首次修复只能有一次**：只允许在高置信、低风险、单点源头时做小范围修复。
2. **没有新证据，不做第二次修复**：首次修复失败后，后续修改必须依赖 `S2` 或 `S3`。
3. **修源头，不修症状**：只改首次偏离的节点，不在下游症状上补丁式修补。
4. **快速诊断必须先看根因追踪和历史线索**：`root-cause-tracing`、同轮 session、memory index 是默认动作，不是可选动作。
5. **埋点必须可清理、低副作用**：前端用 HTTP 日志收集，后端用文件写入；禁止散点 `console.log` 式日志。
6. **修复后必须有确认回合**：任何修复型代码改动后，都必须输出短确认回合，不能用普通总结代替。
7. **session 记录默认产物**：进入埋点、首次修复失败、或方向自疑时，必须立即创建或更新 `.debug/session/{session_id}.md`。

---

## 证据等级

调试时显式列出已有证据等级，避免把猜测包装成结论。

| 等级 | 来源 | 用途 |
|------|------|------|
| S0 | 用户症状描述 | 理解问题、生成初始假设；不算根因证据 |
| S1 | 静态代码推理、类型关系、调用链分析 | 形成根因候选；可支持首次修复判断 |
| S2 | 现有运行时证据：错误堆栈、测试失败、命令输出、网络响应、服务端日志、浏览器控制台、截图/DOM/CSS inspection | 可作为独立证据 |
| S3 | 为当前假设新增的埋点日志或调试探针 | 定位首次偏离点的强证据 |
| S4 | 修复后复现验证、本地测试或用户确认 | 证明修复结果，不反推修复前根因 |

证据要求：
- 首次修复：至少需要 `S0 + S1`，且满足首次修复条件。
- 首次修复失败后：后续修复必须至少包含 `S2` 或 `S3`。
- 高风险 / 跨模块 / 异步 / 缓存 / 接口 / UI 链路复杂问题：优先要求 `S2` 或 `S3`。

---

## 复杂问题分流

先判断问题是不是“必须先看运行时才能知道”的那一类。下面这些形态，默认不要硬做第一刀：

| 问题形态 | 默认路径 |
|----------|----------|
| 单点源头明确、范围小、低风险 | 进入第二步，允许一次首次修复 |
| 异步 / 竞态 / Promise / timer / await 相关 | 先走第三步，优先拿运行时证据 |
| 缓存 / 状态同步 / 多入口写同一状态 | 先走第三步，优先拿运行时证据 |
| UI + 后端联动、跨模块传播链不清 | 先走第三步，先二分定位 |
| 你已经觉得“这刀得先埋点才知道对不对” | 不做第一刀，直接第三步 |

这一步的目标很简单：**先决定是“修一次”还是“先看证据”**。如果已经分不清，就不要继续猜。

---

## 参考文档使用

不要机械重复读取文档。按需读取：

| 场景 | 读取 |
|------|------|
| 每次快速诊断 | `references/root-cause-tracing.md` |
| 需要查看同轮工作台 | `.debug/session/{session_id}.md` 或 `assets/session-record.md` |
| 需要检索历史线索 | `.debug/memory/index.md` + `references/memory-system.md` + `references/memory-patterns.md` |
| 需要注入埋点 | `references/environment-detection.md` + `references/instrumentation-guide.md` |
| 修复后输出确认回合 | `assets/confirmation-round.md` |
| 多轮无进展、证据矛盾、用户纠正方向 | `references/direction-doubt.md` |
| 确认回合通过后需要沉淀高价值经验 | `references/memory-system.md` + `references/memory-patterns.md` + `assets/memory-fix.md` + `assets/memory-pattern.md` |

---

## 记忆架构

记忆分成短期工作台和长期经验。短期记录帮助本轮不丢线索，长期记忆帮助后续更快理解项目；两者都不能替代当前证据。

默认文件结构（项目没有既定约定时使用）：

```text
.debug/
  session/
    {session_id}.md
  memory/
    index.md
    episodes/
    patterns/
    anti-patterns/
```

- **短期 session**：`.debug/session/{session_id}.md`，按 `assets/session-record.md` 创建或更新。进入埋点、首次修复失败、方向自疑时必须存在；确认回合通过后删除或归档。
- **事件记忆**：单个已确认 BUG 的症状、首次偏离点、证据和修复方式。确认回合通过后，按 `assets/memory-fix.md` 写入。
- **模式记忆**：多个事件抽象出的稳定规律。只有重复出现或高度可复用时，按 `assets/memory-pattern.md` 写入。
- **反模式记忆**：多次被证伪的错误假设或误导路径。只在方向自疑或多个 session 反复证伪后写入。
- **索引**：`.debug/memory/index.md` 只做轻量目录，方便 grep 命中；不要写成完整百科。

记忆读取只用于生成假设和提醒历史坑点。当前 `S2/S3` 证据永远优先于历史记忆。

---

## 用户交互原则

默认自动推进。只有阻塞时才问用户，且每次只问最小必要问题。

需要停下询问：
- 触发条件、实际表现、期望行为缺失，无法判断 BUG 是什么。
- 正确行为属于产品 / 业务决策，代码本身无法判断。
- BUG 只能在用户环境复现，或已注入埋点需要用户操作触发。
- 修复后本地无法验证，需要用户确认是否消失。
- 修复后要有一个明确的确认回合：一次问清用户是否已修复，以及现在是否要清理埋点、session 和临时缓存；用户没回应前不要自动清理。

不需要询问：
- 可以继续读代码、跑已有检查、分析调用链或日志。
- 现有证据已经通过门禁，可以直接修源头。
- 埋点日志已经产生，可以直接分析。

不要把用户锁死在固定按钮里。用自然语言给出确认问题，但修复后必须让用户知道三种明确入口：`未修复`、`方案不对`、`修复完毕`。

---

## 调试会话记录

当进入埋点、首次修复失败、或方向自疑时，必须立即创建或更新 `.debug/session/{session_id}.md`，并按 `<skill base directory>/assets/session-record.md` 的结构维护。它是本轮默认短期记忆，不是可选备注；不要等用户主动要求，也不要只把状态留在聊天回复里。

如果当前还没有 `session_id`，立即生成 `debug_YYYYMMDD_HHmmss_随机4位`。后续埋点、日志文件、确认回合、清理脚本都复用同一个 `session_id`。

每次状态变化后更新它，尤其是：
- 当前假设或已排除假设变化
- 新增或清理埋点
- 用户反馈“未修复 / 方案不对 / 修复完毕”
- 日志结论推翻了原调用链
- 从首次修复转入埋点，或从埋点转入方向自疑

这份记录是短期工作台，不等同于长期记忆。

---

## 整体流程

```
用户报告 BUG
    ↓
【快速诊断】理解症状 → 搜索历史记忆(可选) → 重建最短调用链 → 收集现有证据
    ↓
【决策】
    ├─ 高置信 + 低风险 + 单点源头 → 【首次修复】只修一次
    │                                  ├─ 验证通过 → 【确认回合】→ 默认写入记忆 → 清理埋点/临时缓存 → 结束
    │                                  └─ 仍未修好 → 【运行时证据 / 埋点】禁止继续猜
    ├─ 根因不清 / 分支不明 / 依赖运行时状态 → 【运行时证据 / 埋点】
    └─ 证据矛盾 / 多轮无进展 → 【方向自疑】
    ↓
【定位首次偏离点】用 S2/S3 证据确认偏差在哪里首次产生
    ↓
【源头修复】只改源头 → 验证 → 短确认回合 → 默认写入记忆 → 清理埋点/临时缓存
```

---

## 第一步：快速诊断

目标是在最短时间内决定：能否安全首次修复，还是必须观测运行时。

自检：
- [ ] 已复述问题：触发条件、实际表现、期望行为。不完整时只问一个最小问题。
- [ ] 已先读 `references/root-cause-tracing.md`，再做调用链或样式链推理。
- [ ] 如果这是同一轮未结束的 debug，已读取 `.debug/session/{session_id}.md` 并沿用同一个 session ID。
- [ ] 已检索 `.debug/memory/index.md` 和 `.debug/memory/`（如果存在）。命中只作为假设来源；30 秒内无命中就跳过。
- [ ] 已定位异常表现点，并重建最短调用链或 DOM/CSS 链。
- [ ] 已收集现有运行时证据：错误堆栈、测试输出、命令输出、网络响应、日志、截图、DOM/CSS inspection。
- [ ] 已列出证据等级和根因候选。

输出格式：

```text
【快速诊断】
症状：...
调用链/样式链：A → B → C
证据：S0 用户描述；S1 静态代码；S2/S3 ...
候选源头：...
下一步：首次修复 / 埋点 / 方向自疑
```

---

## 第二步：首次修复

首次修复不是固定流程，只是一个受限特例。全部满足才允许：

- [ ] 单一根因候选，不是多个可能原因并列。
- [ ] 用户现象和代码错误能直接对应。
- [ ] 修改范围小，通常 1 个文件、几行到十几行。
- [ ] 不做重构、不改架构、不扩大行为面。
- [ ] 不依赖运行时状态、接口返回、缓存、权限、环境变量才能判断。
- [ ] 能解释偏差传播路径：源头错误如何导致用户看到的症状。

首次修复前说明：

```text
【首次假设】
源头候选：...
静态证据：...
偏差传播：...
本次只做一次小范围修复；如果仍未修好，将进入运行时证据 / 埋点模式。
```

如果你已经觉得“这刀修复需要先埋点才能判断”，不要做第一刀，直接进入第三步。

首次修复失败：
- 立即停止所有继续猜测的代码修改，不要再试第二刀。
- 记录失败信息：改了哪里、原假设是什么、用户反馈是什么。
- 进入第三步，用运行时证据重新定位。

---

## 第三步：运行时证据 / 埋点

先用已有运行时证据，后注入埋点。不要为了埋点而埋点。

优先检查：
- 测试失败输出、错误堆栈、命令输出
- 浏览器控制台、网络请求 / 响应
- 服务端日志、数据库 / 缓存返回
- 截图、DOM/CSS inspection

现有证据仍不足时，注入埋点：
- [ ] 读取 `references/environment-detection.md` 判定客户端 / 服务端。
- [ ] 读取 `references/instrumentation-guide.md` 选择模板。
- [ ] 埋点必须逐字套用对应模板，只替换占位符，不改日志结构、不自创字段、不改 `#region DEBUG` 包裹方式。
- [ ] 如果模板无法直接套用，先重新判定环境和语言，不要临时拼一个“差不多”的版本。
- [ ] 如果已有 session 记录，复用同一个 session ID；否则生成 `debug_YYYYMMDD_HHmmss_随机4位`。
- [ ] 在 `.debug/session/{session_id}.md` 记录假设、埋点位置和待验证问题。
- [ ] 首轮最多 3 处，追加每轮最多 2 处，总共最多 3 轮。
- [ ] 埋点位置按调用链二分选择，不做全量散点。
- [ ] 客户端先检查 `http://localhost:9220/health`，不通则启动日志服务。
- [ ] 注入后清空对应日志，让用户复现一次。

每轮埋点计划整体必须回答：
- 收到了什么？
- 返回了什么？
- 走了哪个分支？
- 对外产生了什么影响？

日志分析：
- 数据在当前节点入参已经偏离 → 向上游继续二分。
- 入参正确但输出 / 副作用偏离 → 当前节点是首次偏离点。
- 日志调用链与静态链不一致 → 修正调用链，不沿错误链路继续。
- 连续两轮未捕获偏差或证据矛盾 → 第六步方向自疑。

---

## 第四步：源头修复

只有满足以下任一条件才修：
- 首次修复条件通过，且本次是第一次小范围修复。
- 已有 `S2` 或 `S3` 指向首次偏离点。

修复前确认：
- [ ] 改的是数据 / 样式首次偏离的源头节点，不是下游症状。
- [ ] 涉及公式 / 算法时，已用实际数据手工验算。
- [ ] 本次修复有新证据支撑；如果是首次修复失败后的再次修复，必须包含运行时证据。

执行：
1. 输出偏差传播路径。
2. 在源头做最小改动。
3. 运行项目约定的最小验证命令，或说明为什么无法本地验证。
4. 若有埋点，确认回合结束后再用清理脚本清理。

---

## 第五步：确认回合

确认回合是修复后的强制动作。只要本轮做了修复型代码改动，就必须执行；不要因为“看起来已经修好”“测试通过”“改动很小”而跳过。

修复后不要直接清理，也不要直接写长期记忆。进入第五步前必须读取 `<skill base directory>/assets/confirmation-round.md`，并按它的 markdown 结构输出 `【确认回合】`。

输出要求：
- 尽量短，通常 6-10 行。
- 只保留三块：`修复`、`验证`、`回复我`；必要时再补一行结果影响。
- `diff` 只有真的需要时才写。
- 只输出一个确认回合，不要把写记忆或清理动作提前执行。
- 最终回复如果不是以 `【确认回合】` 为核心结构，视为流程失败。

响应规则：
- 用户回复“未修复”或等价表达 → 更新 session 记录，回到第三步继续排查；没有 `S2/S3` 新证据前禁止第二次猜测式修复。
- 用户回复“方案不对”或对方向不满意 → 更新 session 记录，进入第六步方向自疑；先复盘，再决定是否回退本轮尝试。
- 用户回复“修复完毕”或明确确认已修好 → 执行确认回合通过后的动作；先写必要记忆，再清理埋点、session 和临时缓存。

---

## 第六步：方向自疑

以下任一满足时立即触发：

| # | 触发条件 |
|---|---------|
| 1 | 用户纠正方向 >= 2 次 |
| 2 | 用户描述与代码静态分析结果根本矛盾 |
| 3 | 埋点日志调用链与用户复现步骤严重不一致 |
| 4 | 同一假设被证据否定 >= 2 次 |
| 5 | 首次修复失败后仍想在无新证据下继续改 |
| 6 | 代码修改 >= 2 次但 BUG 仍在 |
| 7 | 排查超过 5 个回合无实质进展 |
| 8 | 连续两轮埋点未捕获偏差 |
| 9 | 三轮埋点耗尽仍无法定位源头 |

触发后读取 `references/direction-doubt.md`。只回退自己在本次 debug 中做的失败尝试；不要使用 git 命令整体回退用户已有修改。埋点用 `cleanup-debug-blocks.js` 清理。

---

## 确认回合通过后

当用户回复 `修复完毕`，或明确表示已经修好并同意继续处理后：

如果用户暂不确认，就停在这里，保留埋点、session 和记忆草稿，不要执行任何清理动作。

1. 最后更新 `.debug/session/{session_id}.md`：记录用户确认、最终根因、修复文件和是否需要长期记忆。
2. 写事件记忆（默认）。如果明确只是单纯 typo、格式化、显而易见的漏 import 这类低价值修复，可以跳过；否则读取 `references/memory-system.md` 和 `references/memory-patterns.md`，按 `assets/memory-fix.md` 写入事件记忆。
3. 如果这次修复暴露出稳定规律或稳定反例，再按 `assets/memory-pattern.md` 写入模式记忆或反模式记忆，并更新 `.debug/memory/index.md`。
4. 清理埋点和日志（没有埋点就跳过脚本）：
   ```bash
   node <skill base directory>/scripts/cleanup-debug-blocks.js --session-id <id> --files <文件列表> --log-file .debug/logs/<id>.log
   ```
5. 删除或归档 `.debug/session/{session_id}.md` 以及本轮生成的其他临时缓存文件。

低价值修复不要写长期记忆，例如单纯 typo、格式化、显而易见的漏 import。

---

## 工具脚本

```bash
# 健康检查日志服务
curl http://localhost:9220/health

# 启动客户端埋点日志接收服务
node <skill base directory>/scripts/launch-debugger.js

# 修复完成后清理埋点代码和日志
node <skill base directory>/scripts/cleanup-debug-blocks.js --session-id {id} --files {文件列表} --log-file .debug/logs/{id}.log
```
