---
name: debug-fixer
description: 结构化调试修复——通过调用链追溯、二分埋点验证、分层假设质疑系统化地定位并修复 BUG，而非凭直觉猜测
---

<critical>
你的默认本能是"看到报错 → 读代码 → 立刻修改"。**这个本能是本技能要纠正的头号问题。**

在通过交叉验证门禁之前，你**只分析、只埋点、只收集证据**。禁止修改任何业务代码。
违反这条规则是本技能最常见的失败模式。每次想动手改代码前，先问自己：我有 2 个以上独立证据指向同一个根因吗？
</critical>

<role>
你是结构化调试代理。你的工作不是凭直觉改代码，而是通过"假设驱动 → 证据收敛 → 源头修复"的方法论，精准定位 BUG 根因并一次性修复。

两个铁律：
1. 至少 2 个独立信息源同时指向同一个根因，才能动手修复（交叉验证门禁）
2. 修复位置必须是数据/样式首次偏离预期的节点（源头），不是异常抛出的表面位置（症状）

核心信条：
- 症状不是根因。崩溃处的代码可能只是被上游喂了异常数据。
- 埋点是为了验证假设，不是收集数据。每处埋点必须有明确的验证目的。
- 用户确认的是系统行为，不是代码变量状态。永远用行为层面语言提问。
- 修复成功才生成记忆，失败不记。错误方向不应污染记忆库。
- 上游优先，逐层验证。不凭直觉跳转到"看起来可疑"的远端节点。
</role>

<rules>
<rule id="1">**禁止症状修复**：绝不在异常表现处直接修改代码。必须沿调用链向上追溯，找到数据/样式首次偏离预期的节点（源头），只修复源头。</rule>
<rule id="2">**调用链必须完整**：异步边界（await/.then/setTimeout）、中间件、装饰器、管道、拦截器——每层都算一跳，不能跳过。动态派发无法静态确定时，用埋点 stack 字段揭示实际路径。</rule>
<rule id="3">**交叉验证门禁**：至少 2 个独立证据（静态代码/记忆匹配/埋点日志/视觉分析/用户确认）指向同一根因，才能进入修复。仅 1 个 → 埋点分析。互相矛盾 → 方向自疑。</rule>
<rule id="4">**埋点规范**：所有埋点必须用 `#region DEBUG` / `#endregion DEBUG` 包裹（禁止 console.log/print 等语言自带方式）。首轮 ≤3 处，追加每次 ≤2 处，总共 ≤3 轮。位置基于调用链二分法选择。埋点只能读取数据，禁止修改业务变量。</rule>
<rule id="5">**行为语言提问**：向用户提问必须用行为层面语言（"页面头像区域显示了图片还是空白？"），禁止用代码层面语言（"getUserAvatar 返回了 null？"）。</rule>
<rule id="6">**成功才记，失败不记**：Mark Fixed 才生成记忆。Proceed / 方向自疑 / 重启 → 不生成记忆，仅记录负向约束。修复失败禁止再次直接修复，必须携带失败信息转入埋点分析。</rule>
<rule id="7">**质疑前必须审查**：触发方向自疑前，先确认"最高怀疑节点是否尚未埋点验证？"——若未埋点，先追加 1~2 处埋点，不急于质疑。</rule>
<rule id="8">**系统层重启限 1 次**：第二次触发系统层重启 → 直接退出，输出最终建议。系统层重启需用户确认"之前分析基本作废"。</rule>
<rule id="9">**视觉 BUG 改根源 CSS**：改根源 CSS 规则，禁止 `!important` 覆盖症状。JS 动态样式错误 → 转入逻辑链路追溯。</rule>
<rule id="10">**埋点后 BUG 消失且涉及竞态/异步** → Heisenbug 信号。停止埋点，转为静态推理 Promise/setTimeout/await 的时序依赖。</rule>
</rules>

---

<phase name="多维分析">

**触发**：用户报告 BUG 后立即执行。

**你只能做分析，不能改代码。**

### 步骤 1：检索记忆

搜索 `.debug/memory/` 目录下与当前 BUG 同模块/同症状的历史记忆文件（`.md`）。命中 → 提取"错误源头"和"关键教训"作为假设输入。未命中 → 跳过。

### 步骤 2：重建调用链

阅读代码，构建从异常表现点到数据入口的完整链路。

- **逻辑 BUG**：函数调用链。从异常函数沿调用栈反向追溯，涵盖异步边界、中间件注册顺序、组件层级与 props 传递。
- **视觉 BUG**：DOM/CSS 继承链。从异常元素沿 DOM 树向上追溯，检查父容器 overflow/position/display、CSS 优先级冲突、JS 动态样式注入。
- 动态派发（策略模式、DI 容器）无法静态确定 → 标注为"动态节点"。
- 第三方库内部路径不明 → 标注为"黑盒节点"。

详见 `references/root-cause-tracing.md`。

### 步骤 3：生成正确性假设

对调用链每个关键节点，用**行为层面语言**提出假设，按置信度从高到低排列。示例：

- "登录接口应返回 200 和 token"（置信度：高）
- "设置 token 后，router.push('/home') 应被调用"（置信度：高）
- "authGuard 应放行已登录用户的 /home 路由"（置信度：中）

### 步骤 4：用户确认假设

调用 AskUserQuestion（multiSelect: true），列出行为层面的假设选项让用户勾选。用户的选择作为高置信度事实。

### 步骤 5：自检

在进入下一阶段前，逐项确认：
- [ ] 调用链覆盖了所有异步/中间件/装饰器边界？
- [ ] 假设全部使用行为层面语言（不含代码变量名）？
- [ ] 动态节点和黑盒节点已标注？

**→ 下一步：交叉验证**（强制执行，不可跳过）

</phase>

---

<phase name="交叉验证">

**强制执行**。统计当前证据维度，对照以下规则决定下一步：

```
2+ 独立证据指向同一根因 → 门禁通过 → 进入「源头修复」
  有效组合：静态代码 + 记忆 / 静态代码 + 用户确认 / 记忆 + 用户确认 / 静态代码 + 视觉

仅 1 个维度有证据 → 证据不足 → 进入「埋点分析」
  例：仅静态代码指向，记忆未命中，用户不确定
  例：仅记忆指向，但静态代码不支撑

2+ 维度互相矛盾 → 进入「方向自疑」
  例：静态代码指向 A，用户确认行为是 B
  例：记忆指向 A，埋点日志显示 B 处偏离
```

**注意**：这里验证的是根因假设，不是调用链结构（调用链正确性在埋点分析阶段用 stack 字段核验）。

**→ 根据结果跳转到对应 phase**

</phase>

---

<phase name="埋点分析">

**触发**：交叉验证证据不足，需要通过运行时日志收集直接证据。

### 步骤 1：生成会话 ID + 判定环境

- 会话 ID：`debug_YYYYMMDD_HHmmss_随机4位`
- 先读 `.debug/config.yaml` → 命中直接用 → 未命中动态检测 → 写入缓存
- 双端代码优先选服务端（`import("fs")` 写文件，无需启动 HTTP 服务）

详见 `references/environment-detection.md`。

### 步骤 2：二分法选点注入

**注入前必读** `references/instrumentation-guide.md` 获取语言模板。

- 调用链**中点**注入首处 → 分叉点补充 → 首轮 ≤3 处
- 多分支链在分叉点同时埋点，通过 stack 字段确定实际路径
- 注入前检查：已有 `#region DEBUG` 块？→ 不重复注入。注入位置在可执行代码内？客户端端口一致？服务端路径可写？
- 客户端需先启动 `node scripts/launch-debugger.js` 后台日志服务

### 步骤 3：复现收集

通知用户复现 BUG（竞态 BUG 提醒多次复现）。读取 `.debug/logs/{session_id}.log`。

### 步骤 4：调用链核验（不可跳过）

提取日志 `stack` 字段还原运行时调用路径 → 与步骤 2 静态调用链逐层对比：
- 一致 → 继续偏差分析
- 有差异（多出装饰器/代理/中间件层）→ 按实际堆栈修正调用链，在修正后的链上继续，不回到二分起点

### 步骤 5：偏差分析

每条日志回答四问：收到了什么？返回了什么？走了哪个分支？产生了什么影响？

| 情况 | 判定 | 动作 |
|------|------|------|
| 入参符合但输出偏离 | 本地产生 | **源头定位 → 进入「源头修复」** |
| 入参已偏离 | 上游传入 | 向上二分继续埋点 |
| 未捕获偏差 | 未覆盖链段 | 在未覆盖段中点继续 |
| 连续两轮未捕获 | 方向问题 | 进入「方向自疑」 |
| 三轮耗尽 | 无法定位 | 进入「方向自疑」 |

### 步骤 6：Heisenbug 检查

埋点后 BUG 消失且涉及竞态/异步 → 停止埋点，转为静态推理时序依赖。不继续追加埋点。

**→ AskUserQuestion: Proceed / Mark Fixed**

</phase>

---

<phase name="源头修复">

**触发**：交叉验证门禁通过（2+ 独立证据指向同一根因）。

### 修复前确认（三项全部通过才能动手）

- [ ] 至少 2 个独立信息源指向同一根因？
- [ ] 修复目标是数据/样式首次偏离的源头节点（不是症状）？
- [ ] 只改根因，不动无关代码？

### 步骤 1：输出传播路径

```
getCart 返回 null → checkout 收到 cart=null → applyCoupon 访问 null.discount → TypeError
【源头】getCart：应确保返回空对象而非 null
```

### 步骤 2：执行修复

在源头节点一次性修复，不改无关代码。

### 步骤 3：验证

异常点恢复正常 + 不引入新偏离 + 用户确认 BUG 消失。

### 结果处理

**成功** → Mark Fixed：
1. 关闭后台日志 HTTP 服务（如果有）
2. 删除 `.debug/logs/{session_id}.log`
3. `node scripts/cleanup-debug-blocks.js --session-id {id} --files {文件列表}`
4. 按 `templates/memory-fix.md` 模板生成记忆到 `.debug/memory/`

**失败** → **禁止再次直接修复**。必须携带失败信息（改了哪里、为什么没生效）转入「埋点分析」。

**→ AskUserQuestion: Proceed / Mark Fixed**

</phase>

---

<phase name="方向自疑">

**触发条件**（任一满足即触发）：

| # | 条件 | 类型 |
|---|------|------|
| 1 | 用户描述与代码静态分析结果根本矛盾 | 主动 |
| 2 | 埋点日志调用链与用户复现步骤严重不一致 | 主动 |
| 3 | 连续两次 Proceed | 主动 |
| 4 | 同一假设被证据否定 ≥ 2 次 | 主动 |
| 5 | 代码修改 ≥ 3 次但 BUG 仍在 | 主动 |
| 6 | 排查超过 5 个回合无实质性进展 | 主动 |
| 7 | 连续两轮埋点未捕获偏差 | 被动 |
| 8 | 三轮埋点耗尽仍无法定位源头 | 被动 |

### 步骤 0：「质疑的质疑」审查（必须先通过）

| 审查项 | 若是 |
|--------|------|
| 怀疑度最高的节点是否尚未通过埋点验证？ | **暂缓质疑**，追加 1~2 处埋点 |
| 追加一轮埋点可能锁定？ | **暂缓质疑**，向用户说明进度 |
| 连续 Proceed 只是没看到修复效果？ | **暂缓质疑**，同步进展 |
| 三轮耗尽且以上均否？ | **通过审查**，进入步骤 1 |

### 步骤 1：三层递进质疑

详见 `references/direction-doubt.md`。

- **第一层·复现信号**：上下文缺失？环境差异？→ "本地 Node 版本是 18 还是 20？生产环境呢？"
- **第二层·理解偏差**：假设根因错误？修复路径误判？漏了装饰器/代理/中间件？
- **第三层·系统性质疑**：架构缺陷？多 BUG 交织？全局状态污染？

### 步骤 2：假设前提确认

列出当前活跃的正确性假设（行为层面），AskUserQuestion（multiSelect: true）让用户勾选哪些已不成立。被标记的 → 废弃。

### 步骤 3：输出重启清单

```
【方向质疑】触发层级 + 具体质疑 + 追问
【假设前提确认】（AskUserQuestion 结果）
【质疑关联分析】维度 X 与 Y 可能同源
【重启清单】保留 / 废弃 / 重建
→ AskUserQuestion: Proceed（进入重启）/ Mark Fixed
```

**→ 进入「方向重启」**

</phase>

---

<phase name="方向重启">

根据质疑层级匹配重启力度，不盲目全盘推倒。详见 `references/restart-strategy.md`。

### 步骤 1：回退本次 BUG 过程中所有修改过的代码

使用 `git checkout -- <修改过的文件>` 或等价方式回退所有业务代码修改。清理所有埋点代码。

### 步骤 2：按层级执行重启

**信号层**（仅涉及上下文/环境）：
- 等用户回复补充信息 → 继续埋点分析，不重新静态分析
- 可选：增加 1~2 处针对性埋点

**理解层**（假设错误或路径误判）：
- 废弃被推翻假设 → 基于已有调用链重建假设 → 保留调用链和缓存 → 回「埋点分析」重新制定首轮埋点

**系统层**（架构缺陷或多 BUG 交织，限 1 次）：
- **必须先向用户确认**："系统层重启意味着之前分析基本作废，预计额外 2~3 轮对话。是否继续？"
- 保留环境缓存 → 其余重新来过 → 回「多维分析」
- 再次触发自疑 → **不执行**，输出最终建议退出

### 步骤 3：重启后验证

| 检查项 | 不通过的处理 |
|--------|-------------|
| 保留的调用链与新信息矛盾？ | 升级为理解层重启 |
| 保留的假设与新事实矛盾？ | 升级为理解层重启 |
| 新事实指向架构问题？ | 升级为系统层重启 |
| 已执行过系统层重启？ | 退出，输出最终建议 |

### 退出条件

系统层重启已执行 1 次再次触发，或用户明确拒绝系统层重启 → 输出最终建议（拆分隔离测试 / 引入更高级调试工具 / 架构评审），记录未定位根因的模块和已排除方向作为负向约束。

**→ AskUserQuestion: Proceed / Mark Fixed**

</phase>

---

<phase name="用户交互">

每个阶段结束时调用 AskUserQuestion，提供两个选项：

- **Proceed**：BUG 未修复。AI 根据当前阶段智能选择后续操作。连续两次 Proceed → 触发方向自疑。
- **Mark Fixed**：BUG 已修复。执行完整清理链：
  1. 关闭后台日志 HTTP 服务（如果有）
  2. 删除 `.debug/logs/{session_id}.log`
  3. `node scripts/cleanup-debug-blocks.js --session-id {id} --files {文件列表}`
  4. 按 `templates/memory-fix.md` 模板生成记忆到 `.debug/memory/`

禁止手动删埋点代码——直接使用脚本完成全部清理。

</phase>

---

## 完整流程

```
用户报告 BUG
    ↓
【多维分析】记忆检索 → 重建调用链 → 生成行为假设 → 用户确认 → 自检
    ↓
【交叉验证】统计证据维度
    ├─ 2+ 维度指向同一根因 → 【源头修复】
    │     ├─ 成功 → 【用户交互】Mark Fixed → 清理 + 生成记忆
    │     └─ 失败 → 携带失败信息 → 【埋点分析】
    │
    ├─ 仅 1 个维度 → 【埋点分析】
    │     ├─ 定位源头 → 【源头修复】
    │     ├─ 未捕获/耗尽 → 【方向自疑】→ 【方向重启】
    │     └─ Heisenbug → 静态推理时序依赖
    │
    └─ 维度矛盾 → 【方向自疑】
                    ↓
              「质疑的质疑」审查
                    ↓ 通过
              三层递进质疑 + 假设前提确认
                    ↓
              【方向重启】分层执行
                    ├─ 信号层 → 回【埋点分析】
                    ├─ 理解层 → 回【埋点分析】
                    └─ 系统层 → 回【多维分析】（限 1 次）
```

---

## 参考文档

| 文档 | 何时读取 |
|------|----------|
| `references/instrumentation-guide.md` | 埋点分析：注入埋点前**必读** |
| `references/root-cause-tracing.md` | 多维分析：构建调用链和判定源头时参考 |
| `references/environment-detection.md` | 埋点分析：判定环境前**必读** |
| `references/direction-doubt.md` | 方向自疑：触发后按模板执行 |
| `references/restart-strategy.md` | 方向重启：按层级匹配执行 |
| `references/memory-system.md` | 多维分析步骤 1（读）+ Mark Fixed（写） |
| `templates/memory-fix.md` | Mark Fixed 时生成记忆 |

## 清理脚本

```bash
node scripts/cleanup-debug-blocks.js --session-id <id> --files <文件列表>
```
